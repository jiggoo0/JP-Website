/** @format */
import { MetadataRoute } from 'next'

/**
 * 🛰️ METADATA_PROTOCOL: ROBOTS_CONFIG
 * VERSION: 1.1.0 (Domain_Alignment_Security_Patch)
 * ✅ Purpose: ควบคุมการเข้าถึงของบอทเพื่อความปลอดภัยของข้อมูล Verify
 */

export default function robots(): MetadataRoute.Robots {
  // 🌐 กำหนดฐาน URL ตามโดเมนตรวจสอบเอกสาร
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || 'https://jpvisouldocs.website'

  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: [
        '/actions/', // 🔐 ป้องกันการเรียกใช้ Server Actions ภายนอก
        '/lib/', // 🔐 ปกปิดโครงสร้างระบบและ Logic ภายใน
        '/pass/', // 🔐 ป้องกันการดัชนีบัตรผ่านดิจิทัล (Pass)
        '/verify/*', // 🔐 ป้องกันบอทเก็บข้อมูลหน้าผลลัพธ์การตรวจสอบ (สำคัญมาก)
        '/api/', // 🔐 ป้องกันการสแกน Endpoint API
      ],
    },
    sitemap: `${baseUrl}/sitemap.xml`,
  }
}
